# DSH client packages are type/build inputs here; the running WebUI supplies
# their shared module-table identities. Installing their workspace-wide peer
# graph would duplicate the host rather than make this plugin more complete.
legacy-peer-deps=true
