# 贡献指南

欢迎 PR。这个仓库有一条硬规矩：**被验证的东西必须和被交付的东西是同一条代码路径。**
0.1 的教训就是它——实验测的是纯函数，用户拿到的是一份说明书，中间那段没人验过。

## 开发

```bash
npm ci
npm test        # build + typecheck(src/ 与 eval/) + 82 条单元测试
npm run build   # lib/ 随源码提交，git 直装不跑构建
```

`lib/` 是构建产物但**必须提交**：`dsh plugin add github:...` 走 pnpm 克隆安装，
不会替你跑 build。CI 会校验 `lib/` 与 `src/` 同步，改完 `src/` 记得 `npm run build`
并把 `lib/` 一起提交。

## 测试怎么写

- `test/fake-host.mjs` 是一个够真的假 dsh 网关（会话生命周期、`running` 状态、
  history 事件形状、goal / workspace / 模型目录）。迁移链路的任何改动都应该能在它上面验证，
  **不需要真的 host，也不烧 token**。
- `test/cli.test.mjs` 把假 host 挂成真的 HTTP 服务并 spawn 真的 CLI 进程——
  线协议信封、环境变量、退出码、错误文案一起验。这是用户实际走的那条路径。
- 改了压缩取材，请补一条**语义**断言（"超预算时最新的那条还在吗"），
  而不只是"长度没超"。0.1 的预算测试全绿，但砍掉的正好是最该保的部分。

## 加评测题材

题材在 `datasets/`（`test` / `validation` 两个 split）。加之前请先读这一段：

题材的价值在于**不可猜**。打分是子串匹配，如果埋点里的事实模型不看历史也能蒙对
（比如"数据库用 PostgreSQL"对一个电商项目、或者提交语言写成"中文"而探针本身就是中文提问），
那这个事实对所有臂都是白送的，整条曲线的下限会被抬高。

所以：

```bash
BRIDGE_ARM=guess node eval/run.mjs   # 不埋点，直接问探针
```

把你这个题材的猜测基线得分写进 PR 描述。理想是 0/5；超过 1/5 请换值。

## 跑评测

评测烧的是**你自己的** token，不进 CI，需要本地跑着 `dsh web` 且配好模型凭据。

```bash
npm run eval                                   # 全量 26 run
BRIDGE_ONLY='电商' npm run eval                 # 按 id 子串筛
BRIDGE_ARM=all BRIDGE_TO='^(code|minimal)$' node eval/run.mjs 2   # 三臂对照
```

每个 run 会建一个空的临时工作区并在结束时删掉工作区注册（目录留在 `$TMPDIR`）。
不要改回共用工作区：对照臂的 agent 会从磁盘上把埋点事实翻出来，A/B 就没意义了。

## 报数字的口径

- 一个 run 里的 5 个事实**不独立**（全灭是 5 个一起丢），别把 26×5 当成 130 个独立观测。
  按 run 级算均值与方差，或给出置信区间。
- 复述层如果限速截断率高，就标 N/A，不要当结果报。
- A/B 请按目标 preset 拆开：有工具的目标下裸重开能靠翻磁盘蒙混过关，
  和无工具目标下的真失忆是两个机制，平均掉就没意义了。

## 提交信息

用中文或英文都行，写清楚**为什么**而不只是改了什么。
